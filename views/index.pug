extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div

      h1 My Personal Fullstack Portfolio

      p.lead.text-light
        | Projects highlighting my abilities in Fullstack web development. 
        br
        | These project include pure HTML, CSS, Javascript projects. 
        br
        | The examples below show : general html/css/js, page pagination, API calls, an OOP Phrase Guessing game, and a To Do application that uses local storage.
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3
      each val in projects  
        .cell
          a(href=`/project/${val.id}`)
            img.thumbnail(src=`/static/imgs/${val.internal_name}_main.png`)
            h5 #{val.project_name}
        